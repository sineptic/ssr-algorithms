[package]
name = "ssr-algorithms"
description = "spaced repetition algorithms"
license = "MIT"
version = "0.5.0"
edition = "2021"

[dependencies]
s_text_input_f = "0.3"
serde = { version = "1.0", features = ["derive"] }
ssr-core = { version = "0.27" }
